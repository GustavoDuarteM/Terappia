version: '3'
services:
  db:
    image: postgres:13.2
    environment:
      POSTGRES_PASSWORD: password
    ports:
      [5432:5432]
    volumes:
      - ./scripts/init.sql:/docker-entrypoint-initdb.d/init.sql
